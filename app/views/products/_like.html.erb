         <%= "ああああ" %>
          <% if comment.like_user(current_user.id) %>
            <%= button_to "/products/#{comment.product_id}/comments/#{comment.id}/likes", method: :delete, id: "likes-button", params: {product_id: comment.product_id} , remote: true do %>
              <%= image_tag("icon_red_heart.png") %>
              <span>
                <%= comment.good %>
              </span>
            <% end %>
          <% else %>
            <%= button_to "/products/#{comment.product_id}/comments/#{comment.id}/likes", method: :post, params: {product_id: comment.product_id}, id: "likes-button", remote: true do %>
              <%= image_tag("icon_heart.png") %>
              <span>
                <%= comment.good %>
              </span>
            <% end %>
          <% end %>