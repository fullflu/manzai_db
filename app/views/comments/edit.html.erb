<h3>
  <% if @comment.tsukkomi %>
    <%= "#{@comment.product.group.tsukkomi}（つっこみ）のコメントを編集" %>
  <% else %>
    <%= "#{@comment.product.group.boke}（ぼけ）のコメントを編集" %>
  <% end %>
</h3>
<br>
<div class="contents row">
 <% if @prev_comment %>
   <%= "#{@prev_comment.daihon}（直前の発言）" %>
 <% else %>
   <%= "（これより前の発言はありません）" %>
 <% end %>
  <br>
  <br>
   <%= form_for(@comment) do |f|%>
      <div class="daihon_input">
      <%= f.text_area :daihon , class: "product-update"%>
      </div>
     <%= f.submit("更新", class: "btn-update")%>
    <%= button_to "戻る",@product , class: "btn-update"%>

  <% end %>
  <br>
 <% if @post_comment %>
   <%= "#{@post_comment.daihon}（直後の発言）" %>
 <% else %>
   <%= "（これより後の発言はありません）" %>
 <% end %>
</div>

